{
  "name": "agri-cul-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "Agri Price Blockchain MVP with Hardhat smart contract tooling.",
  "scripts": {
    "chain:hardhat": "hardhat node",
    "chain:ganache": "ganache --chain.chainId 1337 --wallet.deterministic",
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "ethers": "^6.13.4",
    "ganache": "^7.9.2",
    "hardhat": "^2.22.19"
  }
}

